---
all:
  children:
    jumphost:
      hosts:
        sirius:
          ansible_host: 10.0.0.7

    management:
      hosts:
        pulsar:
          ansible_host: 10.0.0.8
        lfs-build:
        lyra:

    test:
      hosts:
        test-mgmt:
        test-test:
        test-prod:
