---
all:
  children:
    jumphost:
      hosts:
        sirius:
          ansible_host: 10.0.0.7

    management:
      hosts:
        pulsar:
          ansible_host: 10.0.0.8
        lfs-build:
        pihole:
        hubble:
        alpha-cen:
        proxima-cen:
        vega:

    test:
      hosts:
        test-mgmt:
        test-test:
        test-prod:

    k8s:
      hosts:
        triangulum-alpha:
        triangulum-beta:
        triangulum-gamma:
      vars:
        ansible_user: core
