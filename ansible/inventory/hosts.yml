---
all:
  children:
    management:
      hosts:
        sirius:
          ansible_host: 10.0.0.187
          # TODO: add test-mgmt clone here
    prod:
      hosts: {}
        # TODO: add test-prod clone here
        # test-prod:
        #   ansible_host: 10.10.0.x
    seclab:
      hosts: {}
        # TODO: add test-seclab clone here
        # test-seclab:
        #   ansible_host: 10.20.0.x
